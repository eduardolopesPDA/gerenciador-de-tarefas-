name: CI do gerenciador

on: ["push", "pull_request"]
 
jobs:
  CI-do-gerenciador:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 22.17.1   
    - run: npm ci
    - run: npm run test
